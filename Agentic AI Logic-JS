function generateEmail() {
  const aafpe = document.getElementById("aafpe").value;
  const ls = document.getElementById("ls").value;
  const line = document.getElementById("line").value;
  const template = document.getElementById("template").value;
  const comments = document.getElementById("comments").value;

  const machineReport = `
- Machine AAFPE is in ${aafpe} status
- Machine LS is in ${ls} status
- Machine Line is in ${line} status
`;

  let finalEmail = template.replace("___", machineReport.trim());
  if (comments.trim() !== "") {
    finalEmail += `\n\nAdditional Comments:\n${comments}`;
  }

  // Use innerHTML so line breaks render properly
  const output = document.getElementById("output");
